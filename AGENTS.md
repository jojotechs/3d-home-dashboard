# Prototype Instructions

## Family City decisions

- The approved reference package is ../family-city-design, especially 01-city-overview.png and 08-health-home-levels-v2.png. The latter supersedes health/home in the old landmark board.
- This stage is a real Blender-headless modular model and local interaction prototype. The city must be actual GLB geometry, not a screenshot pretending to be interactive. Procedural Blender mesh generation is explicitly requested by the user.
- Preserve seven district IDs and layout from modeling/scene-manifest.json. Homes and health use cumulative additive facility levels; today progress never removes permanent facilities.
- The v0.8 visual prototype uses clearly marked demo data, local persistence and reversible actions. The next business-development stage must validate real cloud reads/writes and authentication from its first business slice (see the phase-one decisions below). Automatic bank/financial-provider integrations are outside phase one. Keep the two cars independent.
- Blender is installed in /Applications/Blender.app at the user’s request. Prefer it in scripts; exclude runtime installers from handoff archives. Document reproducible commands and verification evidence.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

- Visual refinement approved 2026-09-15: first finish a detailed central-home sample against reference 08, then expand to the rest of the city. The v0.1 primitive blockout is explicitly below the accepted quality bar. Use varied architecture, roof tiles and trims, layered gardens, white fences, paved paths, and fully furnished additive facilities. Validate real geometry from multiple viewpoints.
- The user approved the v0.2 central-home fidelity and requested all remaining districts upgraded to that standard. Preserve the approved home; finish finance, farm, garage, chores, health and learning plus coherent coast/roads in this pass. No further sample approval is needed. Keep all existing state nodes and additive facility slots functional.

- User correction: garage cars were much too large relative to homes and roads. Use one metre scale across the city: two-way roads 6 m, family cars 1.938 m including mirrors by 4.381 m (measured from generated vertices), adults around 1.7 m tall. Future traffic and pedestrians must use the same scale. Resize garage equipment and bays accordingly.

- User correction (finance, 2026-09-15): six similar glass boxes are visually boring. Build a mixed urban fabric with distinct architectural types, staggered heights and setbacks, varied street fronts and a usable public plaza, informed by real city architecture. The v0.4 prototype used three finance milestone levels; the phase-one request below expands finance to ten. Preserve the metre scale and approved other districts.

- Finance art feedback: the user approved the v0.4 prosperous (Lv.3) geometry. Preserve it. Its earlier Lv.1 and Lv.2 depict distinct urban-development stages with different building types, footprints, roofs and street amenities, not truncated versions of the Lv.3 towers. Finance may replace complete stage-specific buildings. The former finance no-regression rule is superseded by round five below: finance follows current saved net savings and can decrease. Health/home retain cumulative additive facilities.

- v0.5 travel/mobility request: add randomized walking pedestrians and road traffic at the existing metre scale. Extend the southeast bridge shown by the user to a new airport island. Add a high-speed rail station at a suitable coastal location. Airport/rail clicks show separate air/train trips with family members, business/holiday purpose, configured departure/end times and live upcoming/in-progress/ended states. Allow local trip creation/editing; retain existing seven districts and earned levels. This explicitly authorizes extending the map and adding two travel destinations.

- Railway feedback: both rail ends must disappear into separate planted mountains with deep arched tunnels, rather than freestanding flat walls. Preserve the cozy SimCity-like terrain style.

- v0.6: add continuous sunrise, morning, afternoon, evening, sunset and night lighting. Follow Beijing time by default; manual light previews and accelerated city-day playback must never change real trip/task time. Night uses readable blue ambient light, varied warm windows, street/airport/station lamps and vehicle lights; preserve approved architecture and metre scale.

- v0.7: real scheduled trip departures animate aircraft taxi/takeoff and trains leaving into tunnels, followed by replacement arrivals and waiting vehicles. Use independently movable Blender geometry at the existing metre scale. Absolute real time controls live departures; daylight previews do not trigger trips, and refreshing never replays historical departures. Explicit animation previews must not edit trips or task records.

- v0.8 feedback: retain warm vehicle cabin windows through material cloning and animation. Keep three distinct full-sized aircraft and two double-ended trains waiting. Vary the selected stand/track and departure end, with stable per-trip choices across refreshes and varied explicit previews. Reserve clear taxi space around parked aircraft; never drive through another parked vehicle.

## Phase-one discovery decisions (2026-09-16)

- The phase-one `grill-with-docs` interview, using `grilling` and `domain-modeling`, is complete. On 2026-09-16 the user gave final confirmation of the full scope, conventional implementation approach and acceptance criteria after Q1–Q26 converged. The agreed specification and visual brief are in GitHub Issue #2 and `docs/specs/phase-one.md`; terminology is in `CONTEXT.md`, consequential trade-offs in `docs/adr/`, and the interview history in `DEVELOPMENT-GRILL.md`. Do not reopen settled decisions without new evidence or user changes. T01 business implementation and actual cloud/in-app-browser acceptance completed on 2026-09-16. The deployed app is https://3d-home-dashboard.vercel.app/; see docs/verification/t01-results.md for evidence and scope. The real household remains empty; use the isolated test households for write tests. Later tickets must read live blockers before starting.
- The user subsequently invoked `to-spec` and confirmed the testing seams: prioritize the real browser-to-cloud workflow, exercise authorization/concurrency through the same cloud business boundary, and reuse pure-state and Blender/multi-view checks for precise amounts and geometry. Issue #2 is the standard specification with extensive user stories and the `ready-for-agent` label; avoid duplicate spec issues or further product interviewing for this conversion.
- During `to-tickets`, the user requested that E2E at the end of each seam preferentially use the in-app browser. Include that preference in every applicable ticket's acceptance criteria and validate each slice independently; precise concurrency/auth checks and model verification still use the appropriate public-interface or Blender checks. Do not defer all E2E to the final integration ticket.
- The user approved the 16-ticket breakdown. Issues #3–#18 are published with `ready-for-agent` and 21 verified native blocking relationships; `docs/planning/phase-one-ticket-breakdown.md` maps slices to issues. Parent spec #2 was not modified. At publication T01/#3 is the only unblocked slice; read live blockers before starting later tickets. Publishing these tickets did not begin implementation.
- Phase one covers real cloud reads/writes, authentication, and `finance` (财务市区), confirmed by the user in round one. “市中心区块” in this phase means the finance district, not `home` (中央住宅). Other districts will receive separate grill sessions.
- Round-one decisions: the first usable release serves the user's own household, while retaining a path to additional households later; public registration is outside the initial release. Prefer free services; discuss necessary costs before any paid commitment. The earlier mainland-network requirement was withdrawn in round three: use ordinary cloud deployment; mainland access quality and ICP-filing preparation are not phase-one selection or acceptance gates.
- Round-two decisions, narrowed by round four: finance initially covers the specified deposits/debts, balance history, trends and city growth; broad asset classification, detailed income/expense ledgers and statement imports are deferred. Login accounts and household members are separate; each operator uses their own account, and members may have no login. Finance is a shared household book visible in full to members granted finance access; personal private books are deferred. Saving is online: only cloud confirmation means saved, and failed submissions retain the current input for retry; offline editing/automatic merge is deferred.
- New finance visual/gameplay requirement (2026-09-16): expand finance growth levels and actual model states from three to ten to encourage saving. Preserve the approved prosperous v0.4 original geometry; the refined ten-level arrangement is in `docs/specs/phase-one.md` and `docs/planning/finance-visual-brief.md`. Calculation scope, thresholds, current-level changes and guide achievement rules are resolved below. This does not expand other districts to ten levels.
- Round-three decisions: use five major development stages with ten visibly distinct geometry levels (not the proposed three major stages). Research mature SimCity/city-builder progression mechanisms as references while retaining original project art. Growth uses the recorded savings net of debt, with the supported balances narrowed below; property/car revaluation and stock-price changes do not directly unlock growth. Authentication uses email/password, household invitations, and email recovery with real delivery verification.
- Round-four simplification (2026-09-16): start the real database empty at finance Lv.1; do not read, seed, import or migrate existing localStorage mock values into real data. Phase-one inputs are bank balances, Alipay/WeChat balances, housing provident-fund balances, and debt; do not build elaborate account/debt classifications or require broad asset valuations. Users edit the finance panel and explicitly click Save; do not add a separate mandatory monthly/full-inventory workflow. Records may be modified at any time. The earlier finance no-regression assumption is superseded by round five below.
- Round-four permissions: the user is the initial household admin; the admin manages invitations and permissions. Members granted finance access may read/write the shared book; others must not receive true finance amounts or wealth levels. Record actor and time for changes.
- Round-five decisions (2026-09-16): finance current level and geometry are a deterministic mapping from the latest saved net savings; allow upgrades and downgrades immediately after saving, with no buffer/hysteresis, grace period or permanent finance unlock. Retain balance history and independently show historical maximum savings/level; correcting an erroneous historical record recalculates historical statistics. The historical maximum must never control the current finance model. This changes finance only, not health/home permanence.
- Phase-one entry UI: two simple lists (balances and debts), editable names and amounts, add/remove entries, RMB only; debt amounts are entered as positive values and subtracted. Show each entry's creator; retain original creator separately from the most recent editor, and attribute each saved historical update to its actor. Creator identity is provenance, not ownership of the money or a private-visibility rule.
- Concurrency uses conventional version/conflict checks and safe retry handling; do not add elaborate collaboration workflows. Backup, export/import and recovery UI are deferred beyond phase one; retain portable business data and versioned migrations, but do not add an unrequested backup service or scheduling requirement.
- Round-six thresholds: finance Lv.1 is the base state (including negative net savings); Lv.2–10 begin at RMB 10,000 / 30,000 / 80,000 / 150,000 / 300,000 / 500,000 / 800,000 / 1,200,000 / 2,000,000. Compare exact stored amounts, permit direct multi-level changes after saving.
- Round-six visual correction: current finance modeling is still too plain. Refine architecture and street details at every level, and add level-specific atmosphere. Lv.9–10 must feel markedly luxurious and prosperous, including architectural spotlights, cyber holographic advertisements and flying advertisements. Preserve original project art, metre scale, approved core geometry and other districts; high-end effects must complement the existing day/night city. This is a stronger visual acceptance bar than the previous sparse roof/garden-only progression proposal.
- Round-six/seven guide decisions: brief upgrade/downgrade feedback, next-target amount, historical-peak display and ten-level guide previews are accepted. Highlight the current level; other levels at or below the valid historical peak remain colored and marked achieved, including after a normal downgrade. Higher levels are greyed/locked but can still be previewed with an explicit unmet/preview label and threshold. Preview never writes financial data or unlocks an achievement. Correcting erroneous history recalculates guide achievement; this never prevents the actual city from downgrading. Empty data starts with the base Lv.1 view and higher entries unmet.
- Validate cloud persistence during the first business implementation, rather than postponing cloud integration until all districts are implemented. The user accepted Vercel (web) + Supabase (Postgres/Auth) + Resend SMTP in round four, free-first. This is the selected development route, not evidence that resources or a sending domain have been connected, and not authorization to purchase services.
- The user supplied `https://github.com/jojotechs/3d-home-dashboard` and explicitly requested repository binding. The workspace now has Git initialized on `main` with that URL as `origin`.
- Add a license-selection task: publish source while reserving commercial use to the owner or explicit owner authorization. Resolve noncommercial use, redistribution, hosted services, contribution rights, and coverage of models/assets before adopting a license. Do not assume a permissive or copyleft open-source license satisfies this requirement.

- Browser preference (2026-09-16): all subsequent browser operations must use the in-app browser. Do not operate the user’s Chrome, which they need for other work.

## Agent skills

### Issue tracker

任务和规格使用 GitHub Issues：`jojotechs/3d-home-dashboard`。见 `docs/agents/issue-tracker.md`。

### Domain docs

单一领域上下文：根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。

- UI architecture correction (2026-09-17): authentication is an app-level concern. Use a standalone login dialog; put account identity and logout in a persistent screen-corner control. Every module entry (map, directory, today/tasks, recommendations) must pass one shared authentication and permission gate: signed-out users see login and resume their intended destination after success, denied users see a separate permission dialog. Keep login/account UI out of feature panels. UI responsibilities must remain decoupled as carefully as code modules. Current living/travel demos require household membership; finance additionally requires the existing finance grant.

- T02 / #4 (2026-09-19): balance/debt lists, changed-row atomic saves, immutable creator attribution and unsaved-exit protection are implemented and deployed on main. See `docs/verification/t02-results.md` for real cloud + two-account in-app-browser evidence and the separate production-domain tool timeout limitation. T03 and later tickets still require a live dependency check.

- T03 mail configuration (2026-09-22): reuse the verified `mail.jojotechs.com` Resend sending domain from roam-together, with app-specific sender `家庭小城 <family@mail.jojotechs.com>`. Keep this app’s Supabase/project/data separate. Setup and current acceptance status are in `docs/verification/t03-cloud-setup.md` and `docs/verification/t03-results.md`; do not equate deployed code with completed human password/email acceptance.

- Member naming feedback (2026-09-22): the new-member name is optional and starts blank. Only when submitted blank (including whitespace), generate a short random nickname made of letters and digits. Preserve explicitly entered names and reuse the generated name on failed-submit retries. Never randomly rename existing members on refresh.

- T03 / #5 (2026-09-22): invitations, finance grants/revocation and email recovery are implemented and deployed. Real delivered invitation and recovery links, user-entered passwords, invited-account cloud read/write, revocation and new-password relogin passed in-app-browser acceptance. See `docs/verification/t03-results.md`. Later tickets must still check live blockers before starting.

- T04 / #6 (2026-09-22): per-row conflict review and safe retry handling are implemented and deployed. Unknown submissions retain the same request until confirmed; successful receipts require a current-book read before further editing. Real cloud races/lost-response recovery and two-account in-app-browser conflict resolution passed, including production-domain save/reload. See `docs/verification/t04-results.md` for evidence and browser fault-injection limits. Later tickets still require live dependency checks.

- T05–T07 / #7–#9 (2026-09-22): cloud history/trends, exact ten-level finance state and historical corrections are implemented and deployed. History and growth refresh from one authorized book; old-record corrections never replace later amounts, latest-record corrections also update the current book, and effective history determines peak/guide achievement. Real cloud race/lost-response/auth checks and in-app-browser local + production acceptance passed; 62 tests and both review axes passed. See `docs/verification/t05-results.md`, `t06-results.md`, `t07-results.md`. Ten-level geometry and the full guide UI remain later tickets; the scene intentionally keeps the base model with an explicit UI notice. Continue checking live dependencies before later work.

- T08 / #10 (2026-09-22): independent real GLBs for finance Lv.1–2 are implemented, deployed and accepted through actual cloud saves/refreshes in the in-app browser. `modeling/finance-levels.json` selects delivered resources; `FinanceDistrict` owns each active level’s geometry, lights and visual clock. Original city/finance v0.4 and legacy lighting assets remain unchanged references; shared live lighting excludes the four old finance inserts. Desktop finance book can sit beside the scene or collapse while preserving drafts and exit dialogs. See `docs/verification/t08-results.md`; later level geometry and full guide remain subsequent tickets, with live blocker checks required.
