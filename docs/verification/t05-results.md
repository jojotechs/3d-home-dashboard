# T05 · 财务历史与趋势

2026-09-22；Issue #7；基线 `1eafa8d`。实时前置 #4 已关闭。

## 实现

财务面板使用“当前账本 / 历史与趋势”两个视图，切换保留当前草稿；从授权的 `get_finance_history` 读取完整云端历史。趋势按保存时间展示，金额、极值和明细使用整数分。空历史不生成点或示例。展开记录可看当时余额、负债、储蓄净额、操作人、北京时间及各项原创建者。

迁移 `202609220002_finance_history.sql` 在写入时保存操作人昵称，包含清空账本的更新；旧记录优先从当时快照补齐，否则使用现有成员昵称。历史已有名称、金额与创建者快照不改变。

## 验证

- TDD：公开 PostgreSQL RPC 测试先因缺少 actor_name 失败，迁移后通过；纯计算测试先缺模块失败，实施后通过。共 2 项专项测试通过，typecheck 和 diff check 通过。全套测试按本轮 T05–T07 完成时统一执行。
- 已将迁移部署到真实 Supabase 项目 `ebffwcnodbusvmsgmnqg`。
- 独立家庭 `6f6cc6cf-49e6-4a3e-989a-c45fd1fe82aa`（T05 历史成长验收家庭），账号角色为“历史验收管理员”；真实家庭未用于写测试。
- In-app browser，localhost:5174 连真实云端：空历史明确空态；16:42:20（北京时间）保存工资卡 1,200.50、负债 200.25，净额 1,000.25；16:42:45 将工资卡改名为银行储蓄、金额 1,500.75 并移除负债。历史显示两次完整更新，旧名称/负债/创建者保留，操作人及北京时间正确。展开明细截图已在任务内展示。
- 刷新再进历史，仍为两点、最高 1,500.75/最低 1,000.25。公开 Supabase RPC 独立核对上述快照，匿名及已登录跨家庭读取均拒绝。
- 本片暂未单独发布前端；本轮完成后统一发布并补充生产验收与双轴 review 结论。

## 发布与双轴审查

提交 `278e9e2`、`5ccad8e`，后续修正 `3ea168d`、`4765e91` 已推送 main；Vercel 对 `4765e91` 返回 Deployment has completed。正式域名 https://3d-home-dashboard.vercel.app/ 的 in-app browser 已登录上述隔离管理员，读到七次一致历史、当前/峰值 Lv.10 与 2,000,000.01。build 与4项 Sites 验证通过；构建仅既有 Three.js 大包提示。

### Standards

硬规则违规0。初次1项结构建议：历史与成长分别刷新导致快照不同步；已统一为面板账本读取并发布场景，定向复查后剩余0。

### Spec

初次1项P2：保存成功但读账失败后的恢复可能丢失等级变化反馈；已保留待处理前等级直至成功读取，并清除旧反馈，复查后剩余0。

各轴最终0未解决发现。浏览器未注入网络故障；读取失败后的反馈恢复经过代码路径审查，不冒称浏览器故障演示。
