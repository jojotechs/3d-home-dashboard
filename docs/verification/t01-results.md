# T01 验收记录

日期：2026-09-16。任务：[Issue #3](https://github.com/jojotechs/3d-home-dashboard/issues/3)。

状态：**T01 完成**。正式部署与真实云端、应用内浏览器验收均通过；后续票仍需读取线上阻塞关系。

- 访问：[家庭小城](https://3d-home-dashboard.vercel.app/)
- Vercel：`jojotechs-projects/3d-home-dashboard`，Hobby；验收代码 `369e6b0`，部署 `6hdcMu9Ty4z93CzNpPvVTfnhMYBY` 显示 Ready。
- Supabase：专用免费项目 `family-city-dashboard` / `ebffwcnodbusvmsgmnqg`，ap-south-1；版本迁移 `202609160001` 已应用。全局注册关闭、邮箱登录保留、匿名登录关闭。Auth Site URL 已设为正式部署地址。
- 正式管理员由维护者安全初始化；真实家庭再次验证余额 0、记录 0、历史 0。凭据仅存在本机忽略文件，没有进入仓库或前端。
- 接口核对的版本、整数分、服务端时间见 [云端证据 JSON](t01-cloud-evidence.json)。

## 应用内浏览器 E2E

2026-09-16 19:37–19:42（北京时间），全部浏览器操作在 in-app browser 完成，使用独立的“T01 浏览器验收家庭”管理员；正式家庭未写入测试数据。

| 场景 | 观察结果 |
| --- | --- |
| 首次登录 | 空账，净额 ¥0.00，Lv.1，提示还没有财务记录 |
| 草稿输入 | 输入“验收工资卡”及 1234.56，已保存净额仍为 0 |
| 首次保存 | 等待真实 RPC 确认后显示 ¥1,234.56、创建者与“已保存到云端” |
| 修改和刷新 | 保存 2345.67，刷新页面、重新打开财务，仍为 ¥2,345.67 |
| 退出和重登 | 退出立刻移除金额，恢复邮箱登录表单；重新登录读回同一记录 |
| 独立标签页 | 新页先要求登录；独立登录后读回 ¥2,345.67。另一页退出不会切换当前页身份 |
| 真实版本冲突 | 第二页保留 3456.78 草稿，第一页先保存 4567.89；第二页旧版本保存被拒，显示记录已变更，草稿 3456.78 保留，不显示保存成功 |
| 冲突恢复 | 第二页读取最新净额 ¥4,567.89，同时保留草稿；核对后保存得到 ¥3,456.78 |
| 历史与稳定 ID | 同一记录共四个版本：123456 / 234567 / 456789 / 345678 分；失败提交未新增历史，创建者始终不变 |
| 退出时刷新 | 退出后立即刷新，再打开财务要求重新登录，没有恢复金额 |
| 原型与几何 | 正式站点载入实际 GLB；旋转视角、日景与夜景可切换，保留街区、机场和高铁。预览后恢复北京时间自动光照 |

会话内已检查并展示真实浏览器截图：夜间登录面板、首次云端保存、冲突后保留草稿、旋转后的日景城市。截图不是云持久化的替代证据；持久化另由真实 Auth/RPC 与四次完整历史核对证明。

浏览器控制接口没有提供网络离线/丢包开关，因此**未声称完成浏览器断网注入测试**。失败保留输入通过真实云端版本冲突验证；安全重试通过实际云端同请求重复调用验证。客户端有 15 秒请求超时，不会把超时当作保存成功。

## 自动验证

- TDD：金额转换、数据库写入/历史、幂等重试、隔离演示财富影响几何均先观察失败，再实现并通过。
- `npm run typecheck`：通过，覆盖新增财务 TypeScript 代码。
- `npm test`：**41/41 通过**，包含财务 SQL 契约、精确金额、原型状态、昼夜、交通/机车动画和 Sites 行为。
- `npm run test:sites`：**4/4 通过**。
- `npm run build`：通过，存在 `dist/client/index.html`、`dist/server/index.js`、`dist/.openai/hosting.json`。仍有 Three 场景单包超过 500 kB 的体积提示。
- `npm run test:cloud`：**1/1 真实云场景通过**。使用两个额外隔离测试家庭，验证邮箱登录、精确保存、重试仅一笔历史、独立会话读取、匿名/跨家庭 RPC 拒绝、跨家庭直接表读为空、直接表写拒绝、并发同版本仅一个成功及退出重登。
- 本地 SQL 测试采用 PGlite；它只证明数据库契约，不被当作托管 Auth/HTTP 的验证。
- 官方 npm 源的空缓存 `npm ci` 成功安装 82 个包。第一次 Vercel 构建因锁文件引用本机公司镜像而卡住，已取消；锁文件下载地址和 `.npmrc` 修正后自动部署 Ready，依赖版本及完整性不变。
- 保留 Sites 包装文件与全部批准模型；本地开发服务由代理运行在 `http://127.0.0.1:5174/`。

## Standards

以批准原型基线 `f4defde` 为固定点，由独立标准审查代理检查 AGENTS、领域文档、ADR 与代码气味基线。没有待修复的标准违规或结构问题；财务隔离、原型保留、公开/管理凭据分离与测试边界符合约定。

## Spec

独立规格审查代理发现并复核修复了认证边界问题：复制标签页可能共享 SDK 广播身份、延迟退出影响新会话、面板重开/刷新恢复待退出会话。现用每页面独立广播频道、标签页凭据适配器、全局退出状态与同步退出标记解决。复核无剩余 P1/P2；部署源与邮箱提供方配置追加复核无问题。实际云与浏览器验收已补齐。

审查汇总：Standards 0 项遗留；Spec 0 项遗留。

## 范围

本票只完成第一笔具名余额的真实闭环与基础契约。列表/债务、邀请与恢复邮件、历史界面、十级成长与模型仍按后续票实施。Resend 邮件未接入，未声称真实邮件已送达；没有购买付费服务。
