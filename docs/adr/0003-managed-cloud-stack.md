# 采用托管的网页、数据库认证和邮件服务

用户在撤回大陆网络质量与备案门槛后，于 2026-09-16 选择 Vercel 托管网页、Supabase 提供 PostgreSQL 与 Auth、Resend 提供邮件的常规部署路线，以免费优先并减少自行维护服务器的工作。保留现有 React/Vite/Three 和 Sites 构建，业务记录与家庭成员独立建模，数据库迁移和权限规则版本化；认证配置、邮件域名及账号迁移仍需单独处理，不能承诺只改连接地址就能更换平台。真实云读写、跨用户权限和邮件送达是实施阶段必须验证的结果，本次选择不表示已创建资源或授权采购。
