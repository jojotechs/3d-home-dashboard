# 财务街区与等级形态验收 · v0.4

> 历史验收说明：本文记录原型各版本的已有验证。正式业务一期的十级财务、云读写、认证和创建者记录尚未实现/验收；新财务升降规则见 [ADR 0004](docs/adr/0004-finance-level-follows-current-savings.md)。


final result: passed

本轮按用户最新反馈执行：先解决六栋玻璃楼外形重复，随后保留用户批准的繁荣街区，重建前两个等级。用户明确要求等级不只改变楼高，因此财务改成完整阶段替换；健康与住宅仍累加设施。

- Lv.1：储蓄所、坡屋顶商铺、合作社、市场棚与花园凉亭；完整低层屋顶、庭院和绿化。
- Lv.2：老银行扩建、L 形办公室、翻新红砖商楼、宽体办公楼、市场骑楼和中层交易所；占地、外立面、屋顶与底层商业均不同于三级。
- Lv.3：保留用户批准的老银行、花园办公楼、红砖商楼、Art Deco 退台塔、斜顶玻璃塔和圆形交易楼。
- 前排低、中层与后排三种地标构成错落的天际线，四面立面都有几何。街边有喷泉、弧形座椅、咖啡外摆与米制行人。
- 修复二级商业裙房重复檐口产生的黑色重叠线。审查了三个等级、背面和四个全城角度，共八张 `renders/v4-*.png`。

## 交互及模型证据

`exports/finance-validation.json`：从空场景重新导入压缩 GLB；18 个财务阶段节点完整，任意等级恰好显示六组建筑，未超出 60 × 44 m 用地或 60 m 高度。财务使用 `minLevel=maxLevel`；没有 `maxLevel` 的住宅、公园设施继续累加。

`exports/browser-finance-v4-qa.json`：实际网页依次预览三级，DOM 记录分别只有 `_level_1`、`_level_2`、`_level_3` 的六组建筑，其余分区状态不变。旋转后立面完整，控制台无新增错误；5/9 日常进度、328600 元示例净资产与已获得 Lv.3 均未改变。六项状态测试包括阶段互斥、预览不改等级、净资产下降不撤销里程碑，全部通过。生产构建及四项 Sites 包装测试通过。

`exports/approved-finance-preservation.json`：已批准的六类繁荣建筑及八个几何辅助函数与批准版本 AST 一致；Lv.3 组合原有三段几何，保持外观。`exports/unchanged-districts-v4.json`：其余六区 GLB 与上一版交付包的 SHA256 全部一致。

## 体积和限制

完整下载包含三个互斥阶段，总计约 102.3 万三角面、6.77 MB；实际同屏只显示一个财务阶段。本机原生 544 px 宽度、财务聚焦视图短时约 60 FPS，80 次绘制；这是单个取景的采样，不代表全城持续性能。全资产面数略高于原始 100 万预算，后续流动角色和车流仍需要 LOD/按区加载规划。生产构建保留约 953 KB 主包提示。

Blender 离线图与网页使用相同几何，渲染光照不同。无交通寻路、角色行走、真实财务连接或多人同步。旧版验收记录保留如下。

---

# 全城模型与交互验收 · v0.3

本轮范围：保留已批准的 v0.2 中央住宅，细化财务、农场、车厂、清理区、健康公园、学习区及海岸道路。此轮资产升级与原有状态交互已完成；没有把静态人物、车辆宣称为已实现的城市交通系统。

## 视觉依据与改进

依据 `references/01-city-overview.png`、`references/08-health-home-levels-v2.png` 和已批准的 `renders/home-v2-level-3.png`。本轮使用真实几何建模，统一瓦片、窗框、金属、木材和植被的风格。

- 六区增加了可从多角度观察的立面、屋顶、设施、家具与场地细节。
- 健康公园 Lv.1 跑道/水池/凉亭；Lv.2 保留一级并加篮球场/健身区；Lv.3 保留前两级并加儿童区/瑜伽平台/服务亭。后侧独立花园是永久绿化。
- 财务楼宇逐段增加实体楼层，门窗不按高度拉伸；学习区的阅读廊架与探索侧翼独立增长。
- 修复铺装交叉处重叠导致的黑斑、钟楼被主屋顶遮挡和沿岸取样不均造成的大片空白。
- 统一交通尺度：家用车实测 1.938 × 4.381 × 1.621 m，行人 1.701 m；路宽 6 m、车道 3 m。车厂按此尺度重建，清运车同步调整。
- 网页保留环境遮蔽、抗锯齿和按状态更新的阴影。Blender 离线图使用 Cycles，网页使用 Three.js；二者光照有差异，几何来源相同。

本轮共 21 张 `renders/v3-*.png`，包括七区、财务/健康/学习等级、相关背面与四个全城角度。旧 `renders/city-*.png` 和非 v3 分区图保留作历史记录。

## 模型与状态验证

`exports/model-validation.json` 记录最终压缩 GLB 从空场景重新导入后的结果。

- 七区 ID、聚焦/标签锚点及所有必需状态节点仍存在。
- 一次农场打卡只改变一个槽位。
- 一次家务完成只减少对应杂物组。
- 蓝车工位变化不影响珊瑚车。
- 健康与住宅等级严格累加。
- 导入后的网格有顶点，变换有限且缩放为正。
- `exports/scale-validation.json` 直接测量几何顶点，确认车辆可容纳于 3 m 单车道内。

## 实际浏览器验收

本地地址 `http://127.0.0.1:4188/`；检查了 1440×900 桌面和 560×900 窄屏，最后恢复了用户窗口尺寸。

- 七区入口及聚焦成功，最高楼顶部完整可见。
- 健康图鉴三个等级实际可见节点依次为 1、3、6 个设施组。
- 阅读打卡仅新增 `plot_0_slot_7`。
- 清理厨房仅移除 `chore_group_0`。
- 完成蓝车胎压检查仅移除 `work_car_a`，`work_car_b` 保留。
- 上述测试操作都已撤销，恢复到测试前的 5/9；预览未写入获得等级。
- 补充常驻撤销入口，提示消失后仍能撤销自己当天的操作。
- 验证旋转按钮、拖动和缩放；窄屏区域面板的地图控件改到右上方，避免被面板挡住。
- 窄屏“回到全城”能完整容纳地图；可选择街区后继续放大观察。
- 最终重新载入检查及控制台记录见 `exports/browser-city-qa.json`。

## 体积和性能

完整模型约 87.6 万三角面，包含所有等级与状态；Draco GLB 约 5.96 MB。解码器在项目内，无外部 CDN 依赖。

通过顶点颜色合并哑光材质，默认桌面视图短时测得约 60 FPS、321 次总绘制和约 167 万累计绘制三角面。后两项包含法线/AO等多次绘制，不等于唯一场景几何数量。原始 220 次绘制与 45 万总览面数预算仍未达到；当前本机流畅，后续加入车流/角色动画时应做 LOD 和持续性能测试。

已执行五项状态测试、生产构建、四项 Sites 包装测试。未部署外网，未接入真实数据。尚无角色路径、车辆交通、风车转动、昼夜或多人同步。


## v0.5 旅行集散地、城市漫游与山体隧道

- 实际 Blender 几何新增机场岛（沿原东南桥连接）和北岸高铁站；轨道两端进入有草坡、松树、海岸岩壁、拱形衬砌和深色内壁的两座独立山体。近景复核清理了轨道入口的岸边碎岩。
- 机场道路改用连接的带状网格，修复转角重叠破面；飞机垂尾改为沿法线挤出。
- 24 辆随机车辆沿既有 6 m 道路靠右行驶，40 位行人沿人行道、机场前庭和高铁站前广场行走。纯模拟测试验证路径连续、靠右、限速、暂停、生成间距和所有路径的横向道路余量。
- 压缩 actor GLB 独立重新导入：车宽 1.938 m、长 4.381 m、高 1.6214 m；行人高 1.7 m；七个关节分组保留。
- 飞机/高铁行程分别显示，按配置的出发和整段行程结束时间计算状态；北京时间输入、自动更新与刷新保存已验证。UI 新增、编辑、错误日期、删除/撤销、状态筛选验证通过，临时测试行程已移除。
- 今日记录仍为用户当时的 6/9。七个既有地区 GLB 的 SHA256 均与 v0.4 一致。
- 450×863 窄屏截图在 `renders/browser/v5-*.png`；1440×900 双列布局通过 DOM 几何检查，无横向溢出。总览会随横竖窗口改变取景方向。
- 15 项状态/模拟测试、4 项 Sites 测试、生产构建、9 区压缩 GLB 重新导入均通过。最终浏览器错误/警告为空。
- 本机最后一次短采样 30 FPS、88 次绘制，采样期间同时运行 Blender 渲染；该数值只作观察，不视为性能基准。
- 飞机与高铁目前为静态停靠模型；行程状态取决于本地设定时间，不接入实时航班或车次。

证据：`exports/browser-travel-v5-qa.json`、`exports/mobility-validation.json`、`exports/v5-preserved-districts.json`、`exports/model-validation.json`、`renders/travel-studies.html`。


## v0.6 昼夜光影

- 新增连续的日出、早上、午后、傍晚、日落与夜晚光照；天空、海水、环境光、太阳方向与阴影随时间平滑变化。夜间保留蓝色环境光，让建筑和道路保持可读。
- 北京时间自动同步为默认模式。六个预设、24 小时时间滑块、两分钟一日漫游和入夜亮灯开关已验证。手动时间与开灯设置刷新后保留；加速漫游刷新后回到自动模式。
- 建筑使用有明暗变化的暖色窗光；路灯有柔和地面光晕，汽车带随行车灯与尾灯，机场有跑道边灯、航站楼灯带和塔台信标，高铁站有站台及隧道入口照明。
- 灯具由 Blender headless 生成，独立 companion GLB 为 75,500 字节。重新导入后验证 86 个唯一灯位、3 个发光材质和 5 个网格对象；保留米制单位。建筑窗光和汽车灯由网页运行时生成。
- 整城、角色及九个独立分区共 11 个 GLB 与 v0.5 SHA256 完全一致。灯光不改已批准的几何及交通尺度。
- 模拟时间跨过午夜、暂停和关闭灯光均通过浏览器验证；与真实行程时间分离。预览前后可见成长节点一致，今日进度保持用户的 6/9，原有行程未编辑。实际日期跨日会刷新今日事务，不移除永久设施，此行为有独立状态测试。
- 实际浏览器截图覆盖住宅六个时段、机场夜景和高铁站夜景；450×863 窄屏可用。1440×900 桌面控制面板边界为 x=28..358、y=157..561.4，无横向溢出，验收后恢复原窗口和自动模式。
- 21 项状态/模拟测试、4 项 Sites 包装测试及生产构建通过。开发热更新增加依赖时产生过一条 React 警告；最终完整刷新后没有新增错误或警告。
- 最后短采样约 75.5 FPS、304 次绘制和 216 万累计绘制三角面。后两项包含 AO 等渲染通道；只记录本机短采样，不作为跨设备性能承诺。
- 光照采用固定的一天艺术曲线，并非按所在地/季节计算的天文日出日落。

证据：`exports/browser-daylight-v6-qa.json`、`exports/lighting-validation.json`、`exports/v6-preserved-models.json`、`renders/daylight-studies.html`。灯具源文件为 `exports/city-lighting-rig.blend`。


## v0.7 到点出发与补班

- 飞机与三节高铁从原静态网格中拆成 `transport_aircraft` / `transport_train` 独立节点。飞机长 25.722 m、翼展 25.801 m；列车长 49.2 m、宽 2.9002 m。原有七个生活分区、灯光模型和人车资产 SHA256 保持不变。
- 飞机路径包含停机位转向、跑道加速、抬轮爬升、空场、进场降落、滑回停机位；完整周期 66 秒。机场停机坪向西补足转弯空间，并增加黄色滑行引导线。高铁从沿海站台进入东侧隧道，下一班从西侧隧道驶出并减速停靠，完整周期 39 秒。
- 列车只沿真实轨道轴线运动；以隧道深处为裁切平面，避免列车穿出山体背面。新增飞机航行灯和列车前后灯，并按昼夜亮度调整。
- 出发按真实绝对时间计算。相同时间同方式的家庭行程合并展示，密集班次依次播放；补班之后恢复候班。历史行程不重播，刷新恢复当前绝对进度。手动动画预览不修改业务时钟，真实出发优先于预览。
- 场站新增候班/出发/补班状态条、“看场站”和“预览出发”。收起行程卡片后留出地图空间；高铁观看视角包含两座山和车站。
- 浏览器创建了两条临时行程，在北京时间 2026-09-16 01:14 同时触发飞机起飞与高铁出站，数据属性证实 `preview:false` 且各自关联正确行程。途中完整刷新后飞机继续返场滑行，高铁已完成补班，未从头播放。两条临时记录随后全部删除，原四条示例行程保留，今日进度仍为自然跨日后的 0/9。
- 27 项状态/模拟测试、4 项 Sites 包装测试、生产构建、整城及交通模型独立导入均通过。验收截图位于 `renders/browser/v7-*.png`，结构/比例报告位于 `exports/transport-validation.json`。

动画只表示家庭行程的出发与场站补班，不查询实际航班/列车运行位置。

最终浏览器控制台错误/警告为空；桌面 1440×900 观看面板无横向溢出，验收后恢复 450×863 原窗口与自动昼夜。证据见 `exports/browser-transport-v7-qa.json`。

## v0.8 候班机群、双向出发与客舱窗光

- 修复动画材质克隆丢失夜间着色回调的问题。Blender 导出独立 `city_cabin_vertex_color` 客舱材质；网页为每架飞机、每列高铁独立控制暖色发光，不依赖易丢失的建筑着色回调。驾驶舱保留深色玻璃。
- 三架客机分别占用 -36、0、36 m 的机位，使用蓝、青、珊瑚色尾翼；均保持 25.722 m 长、25.801 m 翼展。两列双驾驶室高铁分占两条轨道，每列长 52.6 m、宽约 2.9 m。
- 机场增加机位倒推通道、独立滑行道、两端回转区及相应岛岸。最初的弯道测试发现机翼包围轮廓与候班飞机相交，随后将滑行道移至局部 y=-46 m、跑道移至 y=-66 m；最终六种路线全部通过间距检查。灯具位置与新版跑道同步。
- 真实行程通过稳定种子选择车辆及方向，刷新不改变班次的选择；显式预览遍历六种飞机组合、四种高铁组合。飞机一轮 84 秒，列车 39 秒。其余车辆始终候班，补班回到本机位/本轨道。列车方向改变时交换前后灯，双车头几何无需掉头。
- 浏览器实际触发了全部十种组合，确认只有选中车辆移动；起飞与高铁出站截图均记录了运动及停靠车辆的窗光。关灯后五个客舱材质发光为 0，夜间重新开灯后恢复 1.55；早上光照过渡后低于 0.001。完整飞机/列车流程结束后均恢复 3 架/2 列候班。
- 1440×900 桌面活动状态条无横向溢出，面板宽 680 px；窄屏 450×863 也可看到完整状态及预览按钮。机场聚焦改用新岛实际范围的中心，避免为北侧空海域缩小模型。验收后恢复原窗口和自动北京时间，入夜亮灯开启。
- 31 项状态/模拟测试、4 项 Sites 包装测试、生产构建，以及整城、灯光和五个场站角色的压缩 GLB 独立导入检查通过。原七个生活分区与行人/汽车资产 SHA256 保持不变。
- 本轮没有新增、删除或修改行程；四条原示例行程保留，今日进度仍为自然跨日后的 0/9。动画仍是家庭行程的视觉表达，不查询真实航班或列车位置。

证据：`exports/browser-fleet-v8-qa.json`、`exports/transport-validation.json`、`exports/lighting-validation.json`、`exports/model-validation.json`，以及 `renders/browser/v8-*.png`。
